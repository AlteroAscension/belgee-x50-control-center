# Add-on implementation placeholder

This directory is reserved for the future Home Assistant add-on. A
`config.yaml` will be added only together with a runnable image, health check
and Ingress UI; publishing a non-working store entry is intentionally avoided.

